import React, { useRef } from "react";

const NewNav = () => {
    const modal = () => {
        ref.current.click()
    }
     const ref = useRef(null);
     const refClose = useRef(null);

  return (
    <div>
     
    </div>
  );
};

export default NewNav;
